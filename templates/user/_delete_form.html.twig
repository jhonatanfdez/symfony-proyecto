{# Formulario para eliminar un usuario con confirmación SweetAlert2 #}
<form method="post"
      action="{{ path('app_user_delete', {'id': user.id}) }}"
      class="js-delete-form"
      data-swal-title="¿Eliminar usuario?"
      data-swal-text="Esta acción no se puede deshacer.">
    {#
        ATRIBUTOS CLAVE:
        - class="js-delete-form": Activa el listener global de SweetAlert2 en base.html.twig
        - data-swal-title: Personaliza el título del modal de confirmación
        - data-swal-text: Personaliza el mensaje descriptivo del modal

        El formulario NO se enviará hasta que el usuario confirme en el modal SweetAlert2.
        Si cancela, el modal se cierra y no pasa nada.
    #}

    {# Token CSRF para proteger contra ataques de falsificación de peticiones #}
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">

    {# Botón que dispara el submit y activa la confirmación SweetAlert2 #}
    <button class="btn btn-danger">Eliminar</button>
</form>
