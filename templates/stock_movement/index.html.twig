{% extends 'base.html.twig' %}

{% block title %}StockMovement index{% endblock %}

{% block body %}
    <h1>StockMovement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>Tipo</th>
                <th>Descripcion</th>
                <th>Fecha</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stock_movement in stock_movements %}
            <tr>
                <td>{{ stock_movement.id }}</td>
                <td>{{ stock_movement.cantidad }}</td>
                <td>{{ stock_movement.tipo }}</td>
                <td>{{ stock_movement.descripcion }}</td>
                <td>{{ stock_movement.fecha ? stock_movement.fecha|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_stock_movement_show', {'id': stock_movement.id}) }}">show</a>
                    <a href="{{ path('app_stock_movement_edit', {'id': stock_movement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_stock_movement_new') }}">Create new</a>
{% endblock %}
