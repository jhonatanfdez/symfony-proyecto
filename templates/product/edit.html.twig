{#
    Template: Editar Producto Existente (edit)

    PROPÓSITO:
    - Formulario para modificar un producto existente
    - Muestra información de auditoría (quién lo creó, cuándo)
    - Incluye formulario reutilizable _form.html.twig
    - Permite eliminar el producto

    DATOS RECIBIDOS:
    - form: Objeto FormView con el formulario ProductType prellenado
    - product: Objeto Product existente (desde ProductController::edit)

    DIFERENCIAS CON new.html.twig:
    - SÍ muestra info de auditoría (alert azul con creador y fechas)
    - Título dinámico con nombre del producto
    - Label del botón: "Actualizar Producto" vs "Guardar Producto"
    - Incluye botón "Ver detalle" adicional
    - Incluye formulario de eliminación

    CARACTERÍSTICAS ESPECIALES:
    - Alert info con datos de auditoría (SOLO en edit, NO en new)
    - Acceso a relaciones: product.createBy.name
    - Formato de fechas personalizado
    - Lifecycle callback: updatedAt se actualiza automáticamente

    FLUJO:
    1. Usuario modifica campos
    2. Al enviar, ProductController::edit() valida
    3. Si es válido:
       - Doctrine ejecuta UPDATE en BD
       - Lifecycle callback @PreUpdate actualiza updatedAt
       - Flash message de éxito
       - Redirección a listado
    4. Si hay errores: vuelve a mostrar formulario con mensajes
#}
{% extends 'home/base.html.twig' %}

{# Título dinámico: incluye el nombre del producto que se está editando #}
{% block title %}Editar Producto - {{ product.nombre }}{% endblock %}

{% block breadcrumbs %}
  {% include 'partials/_breadcrumbs.html.twig' with {
    items: [
      { label: 'Productos', route: 'app_product_index' },
      { label: 'Editar', active: true }
    ]
  } %}
{% endblock %}

{% block content %}
    {% set cardBody %}
        <h1>Editar Producto</h1>

    {#
        ===== BLOQUE DE AUDITORÍA (SOLO EN EDIT) =====

        Este bloque NO aparece en new.html.twig porque el producto aún no existe.
        Muestra información importante sobre el origen del producto:

        1. Quién lo creó: product.createBy (relación ManyToOne con User)
        2. Cuándo se creó: product.createdAt (timestamp)
        3. Última actualización: product.updatedAt (timestamp, puede ser null)

        ACCESO A RELACIONES EN TWIG:
        - product.createBy → llama a $product->getCreateBy() → retorna objeto User
        - product.createBy.name → llama a $user->getName() → retorna string
        - product.createBy.email → llama a $user->getEmail() → retorna string

        OPERADOR ELVIS (?:):
        {{ product.createBy.name ?: product.createBy.email }}
        Significa: "Si name tiene valor, muéstralo; sino, muestra email"
        Útil porque el campo name puede ser null en algunos usuarios

        FILTRO date():
        {{ product.createdAt|date('d/m/Y') }} → 24/10/2025
        {{ product.createdAt|date('H:i') }} → 14:30

        CONDICIONAL {% if product.updatedAt %}:
        Solo muestra la última actualización si existe (no null)
        updatedAt es null cuando el producto nunca se ha editado
    #}
    <div class="alert alert-info mb-3">
        <strong>📝 Creado por:</strong>
        {{ product.createBy.name ?: product.createBy.email }}
        <span class="text-muted">
            el {{ product.createdAt|date('d/m/Y') }} a las {{ product.createdAt|date('H:i') }}
        </span>
        {% if product.updatedAt %}
            <br>
            <strong>🔄 Última actualización:</strong>
            {{ product.updatedAt|date('d/m/Y H:i') }}
        {% endif %}
    </div>

    {#
        Card Bootstrap con el formulario de edición
        El card-header muestra el nombre y SKU actual del producto
    #}
    <div class="card">
        <div class="card-header">
            {# Muestra nombre y SKU actual (antes de editar) #}
            <h5 class="card-title mb-0">{{ product.nombre }} ({{ product.sku }})</h5>
        </div>
        <div class="card-body">
            {#
                Incluye el formulario parcial reutilizable

                IMPORTANTE: Es el MISMO formulario que en new.html.twig
                La diferencia es que aquí viene prellenado con los valores actuales
                del producto (Symfony hace esto automáticamente)

                PARÁMETROS:
                - button_label: "Actualizar Producto" (en new era "Guardar Producto")

                CAMPOS DEL FORMULARIO (_form.html.twig):
                1. SKU + Nombre (fila 1, 50% cada uno)
                2. Categoría + Activo (fila 2, 50% cada uno)
                3. Descripción (fila 3, 100%)
                4. Precio + Costo + Stock (fila 4, 33% cada uno)

                CAMPOS EXCLUIDOS (no editables):
                - id: clave primaria (automático)
                - createdAt: fecha de creación (solo lectura)
                - updatedAt: se actualiza automáticamente con @PreUpdate
                - createBy: usuario creador (no debe cambiar, auditoría)
            #}
            {{ include('product/_form.html.twig', {'button_label': 'Actualizar Producto'}) }}
        </div>
    </div>

    {# Botones de navegación y acciones #}
    <div class="mt-3">
        {# Botón gris: volver sin guardar cambios #}
        <a class="btn btn-secondary" href="{{ path('app_product_index') }}">Volver al listado</a>

        {# Botón azul: ver la vista detallada (show) del producto #}
        <a class="btn btn-info" href="{{ path('app_product_show', {'id': product.id}) }}">Ver detalle</a>

        {#
            Formulario de eliminación con SweetAlert2
            Incluye el partial _delete_form.html.twig que tiene:
            - Token CSRF
            - Clase js-delete-form para activar modal de confirmación
            - Botón rojo "Eliminar"
            - Mensaje personalizado con nombre del producto
        #}
        {{ include('product/_delete_form.html.twig') }}
    </div>

    {#
        ===== SECCIÓN DE GESTIÓN DE IMÁGENES =====

        Esta sección permite:
        1. Subir nuevas imágenes al producto (columna izquierda)
        2. Ver y eliminar imágenes existentes (columna derecha)

        ARQUITECTURA:
        - Dos cards en columnas (col-md-6 cada una)
        - Formulario de subida apunta a ProductImageController::uploadImages()
        - Cada imagen tiene botón de eliminar que apunta a ProductImageController::deleteImage()

        FLUJO DE SUBIDA:
        1. Usuario selecciona hasta 10 imágenes (HTML5 multiple)
        2. Validaciones: máx 10, 5MB c/u, JPEG/PNG/WEBP
        3. POST a /admin/product/{id}/images (ID del producto)
        4. ProductImageController procesa archivos, genera nombres únicos, guarda en uploads/products/
        5. Crea entidades ProductImage asociadas al producto
        6. Redirecciona de vuelta al edit con flash de éxito

        FLUJO DE ELIMINACIÓN:
        1. Usuario hace clic en botón "Eliminar" de una imagen
        2. SweetAlert2 muestra confirmación (gracias a clase js-delete-form)
        3. Si confirma: POST a /admin/product/image/{id}/delete (ID de la imagen)
        4. ProductImageController elimina registro DB + archivo físico
        5. Redirecciona de vuelta al edit con flash de éxito
    #}
    {#
        DISEÑO ACTUALIZADO (100% DE ANCHO, EN PILA):
        - Se reemplazan las dos columnas (50/50) por dos bloques apilados al 100% de ancho.
        - PRIMERO: Card de "Subir nuevas imágenes" (full width)
        - SEGUNDO: Card de "Imágenes actuales" (full width), debajo del anterior
        - La grilla interna de miniaturas se mantiene como está (2 por fila con col-6)
    #}

    {# BLOQUE 1: Subir nuevas imágenes (100% ancho) #}
    <div class="mt-4">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="fas fa-upload"></i> Subir nuevas imágenes</h5>
            </div>
            <div class="card-body">
                {#
                    form_start con action personalizada:
                    - path('app_product_upload_images', { id: product.id })
                    - Ruta: /admin/product/{id}/images donde {id} = ID del producto
                    - Ejemplo: /admin/product/5/images si product.id = 5
                #}
                {{ form_start(uploadForm, {
                    action: path('app_product_upload_images', { id: product.id }),
                    method: 'POST',
                    attr: {
                        enctype: 'multipart/form-data',
                        'data-turbo': 'false'
                    }
                }) }}
                    {#
                        form_row renderiza el campo 'files' del ProductImageType
                        - Input type="file" con multiple="multiple"
                        - Accept="image/*" para filtrar en el explorador
                        - Validaciones en servidor: Count(max 10), Image(5MB, JPEG/PNG/WEBP)
                    #}
                    {{ form_row(uploadForm.files) }}
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-cloud-upload-alt"></i> Subir imágenes
                    </button>
                {{ form_end(uploadForm) }}
            </div>
        </div>
    </div>

    {# BLOQUE 2: Imágenes actuales (100% ancho), debajo del anterior #}
    <div class="mt-4">
        <div class="card">
            <div class="card-header bg-secondary text-white">
                {#
                    product.productImages es la colección OneToMany
                    Viene del método getProductImages() de la entidad Product
                    Filtro |length cuenta cuántas imágenes tiene
                #}
                <h5 class="mb-0">
                    <i class="fas fa-images"></i>
                    Imágenes actuales ({{ product.productImages|length }})
                </h5>
            </div>
            <div class="card-body">
                {# Condicional: ¿tiene imágenes? #}
                {% if product.productImages|length > 0 %}
                    <div class="row">
                        {#
                            Iterar sobre cada ProductImage del producto
                            Cada 'image' es un objeto ProductImage con:
                            - id: clave primaria de product_image
                            - imageName: nombre original del archivo
                            - imagePath: ruta relativa desde public/ (ej: uploads/products/producto-5-123456.jpg)
                            - position: orden de visualización
                            - createdAt: timestamp de carga
                        #}
                        {% for image in product.productImages %}
                            <div class="col-4 mb-3">
                                <div class="card">
                                    {#
                                        asset() convierte ruta relativa a URL completa
                                        Ejemplo: asset('uploads/products/producto-5-123456.jpg')
                                        → http://localhost/uploads/products/producto-5-123456.jpg

                                        style="height: 150px; object-fit: cover"
                                        - Fija altura para uniformidad
                                        - object-fit: cover recorta/ajusta para llenar el espacio sin distorsión
                                    #}
                                    <img src="{{ asset(image.imagePath) }}"
                                         class="card-img-top"
                                         alt="{{ image.imageName }}"
                                         style="height: 150px; object-fit: cover;">
                                    <div class="card-body text-center p-2">
                                        {# Muestra nombre original del archivo en texto pequeño #}
                                        <small class="d-block text-muted mb-2">{{ image.imageName }}</small>

                                        {#
                                            FORMULARIO DE ELIMINACIÓN CON SWEETALERT2

                                            CLASE js-delete-form:
                                            - Activa listener JavaScript global (en home/base.html.twig)
                                            - Intercepta submit y muestra modal SweetAlert2
                                            - Solo envía si usuario confirma

                                            DATA ATTRIBUTES:
                                            - data-swal-title: título del modal
                                            - data-swal-text: texto descriptivo del modal
                                            - data-swal-confirm: texto del botón de confirmar (opcional)
                                            - data-swal-cancel: texto del botón de cancelar (opcional)

                                            RUTA DE ELIMINACIÓN:
                                            - path('app_product_image_delete', {id: image.id})
                                            - Ruta: /admin/product/image/{id}/delete donde {id} = ID de la imagen
                                            - Ejemplo: /admin/product/image/23/delete si image.id = 23
                                            - IMPORTANTE: NO es el ID del producto, es el ID de ProductImage

                                            TOKEN CSRF:
                                            - csrf_token('delete_image' ~ image.id)
                                            - Genera token único por imagen
                                            - Ejemplo: delete_image23 para la imagen con ID 23
                                            - Validado en ProductImageController::deleteImage()
                                            - Previene ataques CSRF (Cross-Site Request Forgery)
                                        #}
                                        <form method="post"
                                              action="{{ path('app_product_image_delete', {id: image.id}) }}"
                                              class="js-delete-form"
                                              data-swal-title="¿Eliminar imagen?"
                                              data-swal-text="Esta acción no se puede deshacer. La imagen se eliminará permanentemente.">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete_image' ~ image.id) }}">
                                            <button type="submit" class="btn btn-danger btn-sm w-100">
                                                <i class="fas fa-trash"></i> Eliminar
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    {#
                        Estado vacío: el producto no tiene imágenes
                        Muestra mensaje informativo con icono
                    #}
                    <div class="alert alert-info mb-0">
                        <i class="fas fa-info-circle"></i>
                        Este producto no tiene imágenes. Usa el formulario superior para subir algunas.
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
    {% endset %}

    {% include 'partials/_card.html.twig' with {
        title: 'Editar Producto',
        content: cardBody,
        color: 'light'
    } %}
{% endblock %}
