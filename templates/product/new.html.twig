{#
    Template: Crear Nuevo Producto (new)

    PROPÓSITO:
    - Formulario para registrar un nuevo producto en el sistema
    - Incluye formulario reutilizable _form.html.twig

    DATOS RECIBIDOS:
    - form: Objeto FormView con el formulario ProductType
    - product: Objeto Product vacío/nuevo (desde ProductController::new)

    CARACTERÍSTICAS:
    - Card Bootstrap para contener el formulario
    - Label personalizado del botón: "Guardar Producto"
    - NO muestra información de auditoría (el producto aún no existe)
    - El campo createBy se setea automáticamente en el controlador

    FLUJO:
    1. Usuario llena el formulario
    2. Al enviar, ProductController::new() valida
    3. Si es válido:
       - Se ejecuta $product->setCreateBy($this->getUser())
       - Se persiste en BD
       - Flash message de éxito
       - Redirección a listado
    4. Si hay errores: vuelve a mostrar formulario con mensajes
#}
{% extends 'home/base.html.twig' %}

{% block title %}Nuevo Producto{% endblock %}

{% block breadcrumbs %}
  {% include 'partials/_breadcrumbs.html.twig' with {
    items: [
      { label: 'Productos', route: 'app_product_index' },
      { label: 'Nuevo', active: true }
    ]
  } %}
{% endblock %}

{% block content %}
  {% set cardBody %}
    {# Formulario principal del producto CON sección de imágenes integrada #}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'data-turbo': 'false', 'enctype': 'multipart/form-data'}}) }}

    {# Validaciones globales igual que _form.html.twig #}
    {% if form.vars.submitted and not form.vars.valid %}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong>⚠️ Hay errores en el formulario</strong>
            <p class="mb-2">Por favor, corrige los siguientes problemas:</p>
            <ul class="mb-0">
                {% for child in form.children %}
                    {% if child.vars.errors|length > 0 %}
                        {% for error in child.vars.errors %}
                            <li><strong>{{ child.vars.label }}:</strong> {{ error.message }}</li>
                        {% endfor %}
                    {% endif %}
                {% endfor %}
                {% if form.vars.errors|length > 0 %}
                    {% for error in form.vars.errors %}
                        <li>{{ error.message }}</li>
                    {% endfor %}
                {% endif %}
            </ul>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endif %}

    {# Campos del producto (igual que _form.html.twig) #}
    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.sku, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.nombre, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6 mb-3">
            {{ form_row(form.categoria, {'attr': {'class': 'form-select'}}) }}
        </div>
        <div class="col-md-6 mb-3">
            {{ form_row(form.activo) }}
        </div>
    </div>

    <div class="mb-3">
        {{ form_row(form.descripcion, {'attr': {'class': 'form-control', 'rows': 4}}) }}
    </div>

    <div class="row">
        <div class="col-md-4 mb-3">
            {{ form_row(form.precio, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-4 mb-3">
            {{ form_row(form.costo, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="col-md-4 mb-3">
            {{ form_row(form.stock, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    {# Sección de imágenes integrada DENTRO del mismo formulario #}
    <div class="mt-4 mb-3">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="fas fa-upload"></i> Agregar imágenes (opcional)</h5>
            </div>
            <div class="card-body">
                <p class="text-muted small mb-3">
                    Puedes subir hasta 10 imágenes para el producto. También podrás agregar más imágenes después de crear el producto.
                </p>
                {{ form_row(uploadForm.files) }}
            </div>
        </div>
    </div>

    {# Botón de guardar #}
    <div class="mt-3">
        <button class="btn btn-primary">Guardar Producto</button>
    </div>

    {{ form_end(form) }}

    <div class="mt-3">
      <a class="btn btn-secondary" href="{{ path('app_product_index') }}">Volver al listado</a>
    </div>
  {% endset %}

  {% include 'partials/_card.html.twig' with {
    title: 'Crear Nuevo Producto',
    content: cardBody,
    color: 'light'
  } %}
{% endblock %}
